import AnimateWraper from '../../components/transition/AnimateWraper'

const NetDevices = () => {
	return (
		<AnimateWraper>
			<div>NetDevices</div>
		</AnimateWraper>
	)
}

export default NetDevices
